# Database

This folder contains starter SQL files for the Inventory Management System.

- `schema.sql` is reserved for table definitions.
- `seed.sql` is reserved for sample development data.

The files are intentionally minimal so database design can be expanded during implementation.
