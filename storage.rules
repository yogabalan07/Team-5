rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    function isSignedIn() {
      return request.auth != null && request.auth.uid != null;
    }

    function isEnabled() {
      return isSignedIn() && request.auth.token.disabled != true;
    }

    // Profile photos / user uploads live under users/{uid}/.
    match /users/{uid}/{allPaths=**} {
      allow read: if isSignedIn();
      allow write: if isEnabled() && request.auth.uid == uid;
    }

    // Everything else is not writable by clients.
    match /{allPaths=**} {
      allow read: if isSignedIn();
      allow write: if false;
    }
  }
}