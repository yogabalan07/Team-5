# Inventory Management System

Starter full-stack project structure for a college Inventory Management System.

- `src/` contains the React frontend starter files.
- `backend/` contains the Java Spring Boot Maven backend starter files.
- `database/` contains starter SQL schema, seed, and notes.

This project intentionally includes placeholder pages and empty business logic areas so it can grow cleanly during implementation.
