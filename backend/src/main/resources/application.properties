# Spring Boot application configuration with Clever Cloud database connection.
spring.application.name=inventory-management-backend
server.port=8080

# Clever Cloud Database Configuration
spring.datasource.url=${DB_URL:jdbc:mysql://bz58dt6f4ypdlmtgr532-mysql.services.clever-cloud.com:3306/bz58dt6f4ypdlmtgr532}
spring.datasource.username=${DB_USERNAME:urwqfcd0yvwfhmhr}
spring.datasource.password=${DB_PASSWORD:OnhBsTU6JujYP38qOd4C}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA and Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=${JWT_SECRET:your-secret-key-change-this-in-production-environment-variables}
jwt.expiration=${JWT_EXPIRATION:86400000}

# CORS Configuration
spring.web.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
